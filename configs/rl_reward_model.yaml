model:
  model_name_or_path: Qwen/Qwen2.5-0.5B

data:
  train_file: data/rl/preference_train.jsonl
  max_seq_length: 512

training:
  output_dir: outputs/reward_model
  num_train_epochs: 1
  per_device_train_batch_size: 1
  learning_rate: 1.0e-5
  logging_steps: 5

